.esn-attachment-default-viewer
  .left
    a(target='_blank', ng-href='{{attachment.url}}', title="{{ 'Open in a new tab' | translate }}")
      esn-attachment-icon(type='attachment.contentType')
  .right
    h4 {{attachment.name}}
    .info {{ 'Size' | translate }}: {{attachment.length | bytes}}
